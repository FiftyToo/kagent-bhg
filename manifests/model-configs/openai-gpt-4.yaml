apiVersion: kagent.dev/v1alpha2
kind: ModelConfig
metadata:
  name: openai-gpt-4
  namespace: kagent
spec:
  provider: openai
  model: gpt-4-turbo
  apiKeySecret:
    name: openai-api-key
    key: api-key
